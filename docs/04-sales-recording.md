# チケット04: 売上記録機能

## 概要
解釈された売上情報をGoogleスプレッドシートに記録する機能を実装する

## TODO
- [ ] `record_gym_sale` ツールの実装
  - [ ] 引数の受け取り
  - [ ] データバリデーション
  - [ ] スプレッドシートへの書き込み処理
- [ ] 列ごとの書き込みロジック実装
  - [ ] C列（日）: 数値のみ抽出（「12/28」→「28」）
  - [ ] D列（販売者）: M列のトレーナー名リストから抽出、不明時は「服部誉也」
  - [ ] E列（決済方法）: PayPal, PayPay, 現金 等
  - [ ] F列（商品・サービス名）: 月4回プラン, パーソナルトレーニング 等
  - [ ] G列（数量）: 数値、不明時は1
  - [ ] H列（単価（税抜）): 税込価格から逆算
- [ ] B列のチェックボックスは空欄（未チェック）で保持
- [ ] I列・J列は操作しない（数式保護）
- [ ] エラーハンドリング
- [ ] 書き込み成功/失敗のログ出力

## 関数仕様
```python
def record_gym_sale(
    day: int,                      # 日付（数値）
    seller: str,                   # 販売者名
    payment_method: str,           # 決済方法
    product_name: str,             # 商品・サービス名
    quantity: int,                 # 数量
    unit_price_excl_tax: float    # 単価（税抜）
) -> dict:
    """
    売上情報をスプレッドシートに記録する

    Returns:
        dict: {"success": bool, "row": int, "message": str}
    """
```

## 成果物
- [ ] `record_gym_sale` 関数
- [ ] データバリデーションモジュール
- [ ] ユニットテスト
- [ ] 統合テスト

## 依存関係
- チケット02（Google Sheets API連携）完了後
- チケット05（税計算ロジック）完了後

## 関連ドキュメント
- requirements.md の「3. スプレッドシート仕様」「5. 実装ツール③」

## ステータス
⬜ 未着手
