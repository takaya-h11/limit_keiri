# Google Sheets API
# スプレッドシートID（URLから取得）
# https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit
GOOGLE_SHEET_ID=1oklcKDJ3QNVJ3WXawrr2c1oi0mrjyp39HwvyqcTwniE

# Google サービスアカウント認証（2つの方法のいずれか）
# 方法1: ローカル開発用 - ファイルパス
SERVICE_ACCOUNT_FILE=config/service-account.json

# 方法2: クラウドデプロイ用 - Base64エンコードされたJSON
# 以下のコマンドでBase64エンコード:
# Linux/Mac: cat config/service-account.json | base64 -w 0
# Windows: [Convert]::ToBase64String([System.IO.File]::ReadAllBytes("config\service-account.json"))
# GOOGLE_APPLICATION_CREDENTIALS_JSON=eyJ0eXBlIjoi...

# オプション: ログレベル（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# MCP Server Transport Mode
# - stdio: ローカル開発用（Claude Desktop等）
# - sse: クラウドデプロイ用（Render, Railway等）
MCP_TRANSPORT=stdio

# クラウドデプロイ用ポート設定（SSE mode時のみ使用）
# PaaSプラットフォームが自動的にPORTを設定します
PORT=8080

# ホスト設定（SSE mode時のみ使用）
HOST=0.0.0.0
